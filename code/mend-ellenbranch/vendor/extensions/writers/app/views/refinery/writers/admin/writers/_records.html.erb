<%= render 'refinery/admin/search_header', :url => refinery.writers_admin_writers_path %>
<div class='pagination_container'>
  <% if @writers.any? %>
    <%= render 'writers' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
